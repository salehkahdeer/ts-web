@model Seed.TriumphSkill.Models.Village

@{
    ViewBag.Title = "Delete Village";
}

@using (Html.BeginForm("Delete", "Village", FormMethod.Post, new { @class = "form-horizontal" }))
{
    <div class="panel panel-default">
        <div class="panel-heading"><span class="glyphicon glyphicon-minus-sign" aria-hidden="true" style="margin-right: 4px;"></span>Confirm Deletion</div>
        <div class="panel-body">
            <legend>Are you sure you want to delete the village?</legend>
            <div class="col-lg-6">
                <div class="form-group">
                    @Html.LabelFor(m => m.Name, new { @class = "col-lg-2 control-label" })

                    <div class="col-lg-10">
                        <label class="control-label"><strong>@Model.Name</strong></label>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <button type="submit" class="btn btn-primary">Confirm</button>
                        @Html.ActionLink("Cancel", "Index", "Village", new { @class="btn btn-default" })
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts {

}